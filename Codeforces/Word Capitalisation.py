line=input() #asks for a string. String by default in python is indexed like lists.
print(line[0].upper()+line[1:]) #line[0].upper() capitalizes the first letter of the string. +line[1:] add the string except the 0 indexed first letter.
