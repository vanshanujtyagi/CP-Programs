class Solution: #leetcode default
    def getConcatenation(self, nums: List[int]) -> List[int]: #leetcode default for nums=list(map(int,input().split()))
        ans=nums+nums #concatenates or just add the list/array two times into one.
        return ans #prints the concatenated list/array
